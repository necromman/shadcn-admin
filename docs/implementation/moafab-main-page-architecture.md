# MOAFAB 메인 페이지 아키텍처 문서

## 📋 개요

MOAFAB 홈페이지 개편의 메인 페이지 구현을 위한 상세 아키텍처 문서입니다. 기존 디자인 시스템 컴포넌트를 최대한 활용하면서 개발자 설정 기능을 포함한 완전한 메인 페이지를 구성합니다.

### 관련 문서
- [설계 문서](./moafab-redesign-design.md)
- [진행 상태](./moafab-redesign-progress.md)
- [사이트 분석](./moafab-current-site-analysis.md)
- [요구사항](../요구사항_상세분석_SFR001-008.txt)

## 🏗️ 메인 페이지 레이아웃 구조

```
┌─────────────────────────────────────────────────────┐
│  Pre-Header (유틸리티 메뉴 + 개발자 설정 버튼)          │
├─────────────────────────────────────────────────────┤
│  Header (로고 + 메인 네비게이션)                       │
├─────────────────────────────────────────────────────┤
│  Hero Section (메인 비주얼 슬라이더)                   │
├─────────────────────────────────────────────────────┤
│  Equipment Search (공정장비 검색)                     │
├─────────────────────────────────────────────────────┤
│  Notice Section (공지사항/보도자료)                    │
├─────────────────────────────────────────────────────┤
│  Quick Menu (빠른 메뉴/서비스 바로가기)                 │
├─────────────────────────────────────────────────────┤
│  Institution Partners (참여기관 소개)                  │
├─────────────────────────────────────────────────────┤
│  Footer (하단 정보 및 관련 링크)                       │
└─────────────────────────────────────────────────────┘
│                                                      │
│  [개발자 설정 드로어] - Ctrl+Shift+D 또는 ⚙️ 클릭       │
└─────────────────────────────────────────────────────┘
```

## 🎨 컴포넌트 매핑 상세

### 1. Pre-Header + 개발자 설정
```typescript
// 구성
- 기본: ds-announcement-bar.tsx
- 추가: 개발자 설정 버튼 (톱니바퀴 아이콘)
- 참조: library/components/dev-settings-panel.tsx

// 기능
- 로그인/로그아웃
- 회원가입
- 언어 선택 (KR/EN)
- 서비스 이용료 링크
- ⚙️ 개발자 설정 버튼 (우측 상단 고정)

// 개발자 설정 드로어 포함 사항
- 캐러셀 옵션 (자동재생, 속도, 효과)
- 공정장비 검색 필터 설정
- 공지사항 표시 개수
- 테마 설정 (라이트/다크)
- 언어 설정
- 레이아웃 옵션
```

### 2. Header + Navigation
```typescript
// 기본 컴포넌트
- ds-header-enterprise.tsx (완전 활용)

// MOAFAB 메뉴 구조
const navItems = {
  "팹서비스 소개": [
    "사이트 이용방법",
    "팹서비스 절차",
    "기관별 종합현황"
  ],
  "팹서비스 신청": [
    "상담 신청",
    "견적 신청",
    "서비스 신청",
    "장비별 신청",
    "모아팹 신청",
    "마이칩 신청"
  ],
  "팹서비스 현황": [
    "상담 현황",
    "견적 현황",
    "서비스 현황",
    "서비스 모니터링",
    "정산 현황"
  ],
  "고객센터": [
    "공지",
    "보도자료",
    "문의",
    "FAQ"
  ]
}

// 특징
- 2단계 드롭다운 메뉴
- 모바일 Sheet 네비게이션
- 활성 메뉴 하이라이트
- 키보드 접근성
```

### 3. Hero Section
```typescript
// 기본 컴포넌트
- ds-carousel.tsx (완전 활용)

// 슬라이드 구성
const slides = [
  {
    title: "나노팹 서비스 플랫폼",
    subtitle: "첨단 반도체 공정 지원",
    description: "6개 주요 기관의 장비를 한 곳에서",
    cta: { text: "서비스 신청", link: "/apply" }
  },
  // ... 3-5개 슬라이드
]

// 개발자 옵션 (드로어에서 제어)
- autoPlay: boolean
- interval: number (3000-10000ms)
- effect: 'slide' | 'fade'
- showIndicators: boolean
- showNavigation: boolean
```

### 4. Equipment Search (공정장비 검색)
```typescript
// 신규 구현 + 참조
- 참조: ds-integrated-search.tsx
- 신규: moafab/components/equipment-search.tsx

// UI 구성
interface EquipmentSearchProps {
  // 기관 필터 (체크박스)
  institutions: ["NINT", "NNFC", "KANC", "ISRC", "ETRI", "DGIST"]
  
  // 공정 필터 (드롭다운)
  processes: [
    "에피공정", "산화공정", "포토리소그래피",
    "식각공정", "증착공정", "배선공정",
    "EDS", "패키징", "분석 및 기타"
  ]
  
  // 키워드 검색
  searchPlaceholder: "장비명, 모델명, 사양 검색"
}

// 개발자 옵션
- defaultInstitutions: string[] (기본 선택 기관)
- searchDebounce: number (검색 지연 시간)
- showResultCount: boolean
- resultLayout: 'grid' | 'list'
```

### 5. Notice Section
```typescript
// 기본 컴포넌트
- ds-board.tsx 또는 ds-notice-preview.tsx

// 탭 구성
const tabs = ["공지사항", "보도자료", "FAQ"]

// 개발자 옵션
- itemsPerTab: number (3-10)
- showDate: boolean
- showBadge: boolean (신규/중요 표시)
- refreshInterval: number (자동 새로고침)
```

### 6. Quick Menu
```typescript
// 참조 컴포넌트
- ds-action-cards.tsx
- ds-library-quick-menu.tsx

// 메뉴 항목
const quickMenuItems = [
  { icon: "📝", title: "서비스 신청", link: "/apply" },
  { icon: "🔍", title: "장비 검색", link: "/search" },
  { icon: "📊", title: "진행 현황", link: "/status" },
  { icon: "💰", title: "견적 요청", link: "/quote" },
  { icon: "📞", title: "상담 예약", link: "/consult" },
  { icon: "📚", title: "이용 가이드", link: "/guide" }
]

// 개발자 옵션
- columns: number (2-4)
- showIcons: boolean
- cardStyle: 'bordered' | 'filled' | 'ghost'
```

### 7. Institution Partners
```typescript
// 기본 컴포넌트
- ds-sponsors-slider.tsx 또는 ds-partners-cards.tsx

// 기관 데이터
const institutions = [
  { id: "NINT", name: "나노융합기술원", logo: "..." },
  { id: "NNFC", name: "나노종합기술원", logo: "..." },
  // ... 6개 기관
]

// 개발자 옵션
- displayMode: 'slider' | 'grid'
- autoScroll: boolean (슬라이더 모드)
- showDescription: boolean
```

### 8. Footer
```typescript
// 기본 컴포넌트
- ds-footer-enhanced.tsx 또는 ds-footer-library.tsx

// 섹션 구성
- 회사 정보
- 빠른 링크
- 고객 지원
- 연락처
- 저작권 정보

// 수정 범위: 최소 (텍스트/링크만 변경)
```

## 🔧 개발자 설정 패널 통합

### 구현 방식
```typescript
// src/features/moafab/context/dev-settings-provider.tsx
export const MoafabDevSettingsProvider = ({ children }) => {
  // 개발자 모드 상태 관리
  const [isDevMode, setIsDevMode] = useState(false)
  const [settingsOpen, setSettingsOpen] = useState(false)
  
  // 각 섹션별 설정 상태
  const [carouselSettings, setCarouselSettings] = useState({...})
  const [searchSettings, setSearchSettings] = useState({...})
  const [noticeSettings, setNoticeSettings] = useState({...})
  
  // Ctrl+Shift+D 단축키 처리
  useEffect(() => {
    const handleKeyDown = (e) => {
      if (e.ctrlKey && e.shiftKey && e.key === 'D') {
        setSettingsOpen(true)
      }
    }
    window.addEventListener('keydown', handleKeyDown)
    return () => window.removeEventListener('keydown', handleKeyDown)
  }, [])
  
  return (
    <DevSettingsContext.Provider value={{...}}>
      {children}
      <DevSettingsPanel open={settingsOpen} onOpenChange={setSettingsOpen} />
    </DevSettingsContext.Provider>
  )
}
```

### 개발자 설정 UI
```typescript
// 톱니바퀴 버튼 위치
- Pre-Header 우측 끝 고정
- z-index: 50 (항상 상위)
- 툴팁: "개발자 설정 (Ctrl+Shift+D)"

// 드로어 구조
<Sheet>
  <SheetContent className="w-[400px] overflow-y-auto">
    <Tabs>
      <TabsList>
        <TabsTrigger>일반</TabsTrigger>
        <TabsTrigger>캐러셀</TabsTrigger>
        <TabsTrigger>검색</TabsTrigger>
        <TabsTrigger>공지사항</TabsTrigger>
        <TabsTrigger>레이아웃</TabsTrigger>
      </TabsList>
      {/* 각 탭별 설정 UI */}
    </Tabs>
  </SheetContent>
</Sheet>
```

## 📦 파일 구조

```
src/features/moafab/
├── pages/
│   └── home.tsx                    # 메인 페이지
├── components/
│   ├── layout/
│   │   ├── moafab-header.tsx      # 헤더 (ds-header-enterprise 확장)
│   │   ├── moafab-pre-header.tsx  # Pre-header + 개발자 버튼
│   │   └── moafab-footer.tsx      # 푸터
│   ├── sections/
│   │   ├── equipment-search.tsx    # 공정장비 검색 (신규)
│   │   ├── notice-section.tsx      # 공지사항 섹션
│   │   ├── quick-menu.tsx          # 빠른 메뉴
│   │   └── partners-section.tsx    # 참여기관
│   └── dev-settings/
│       ├── dev-settings-panel.tsx  # 개발자 설정 패널
│       └── dev-settings-button.tsx # 톱니바퀴 버튼
├── context/
│   └── dev-settings-provider.tsx   # 개발자 설정 Context
└── hooks/
    └── use-dev-settings.ts         # 개발자 설정 Hook
```

## ✅ 기능 보존 체크리스트

### 필수 유지 사항
- [x] 모든 디자인 시스템 컴포넌트의 기본 기능
- [x] 개발자 설정 드로어 (톱니바퀴 버튼)
- [x] Ctrl+Shift+D 단축키
- [x] 반응형 디자인
- [x] 다크모드 지원
- [x] 키보드 접근성
- [x] 모바일 터치 지원

### 확장 가능 사항
- [x] MOAFAB 전용 메뉴 구조
- [x] 공정장비 검색 필터
- [x] 기관별 커스터마이징
- [x] 개발자 옵션 추가

## 🚀 구현 우선순위

1. **Phase 1**: 기본 레이아웃
   - MoafabDevSettingsProvider 구현
   - Pre-header + 개발자 버튼
   - Header + Navigation
   - Footer

2. **Phase 2**: 핵심 섹션
   - Hero Carousel
   - Equipment Search
   - Notice Section

3. **Phase 3**: 보조 섹션
   - Quick Menu
   - Institution Partners
   - 개발자 설정 패널 완성

4. **Phase 4**: 최적화
   - 성능 튜닝
   - 접근성 검증
   - 반응형 테스트

## 📝 참고사항

- 모든 컴포넌트는 디자인 시스템을 기반으로 하되, MOAFAB 특화 기능만 추가
- 개발자 설정은 로컬스토리지에 저장하여 세션 간 유지
- 프로덕션 빌드에서는 개발자 설정 버튼 숨김 가능 (환경변수로 제어)
- 기존 library 시스템과 완전 분리하여 독립적 운영